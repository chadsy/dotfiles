#!/usr/bin/env bash
declare -F _trace_it &>/dev/null && _trace_it $DOTFILES_HOME/docker/docker_profile.sh

# ----------------------------------------------------------------------
# alias todock="cd ~/docker-local"
# alias dock="docker-compose up -d "
# alias undock="docker-compose down"
